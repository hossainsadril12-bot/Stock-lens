# StockLens — project instructions

Caveman mode is the user's default (level: full). Readability overrides terseness — small chunks, easy words, examples, no emoji/icons.

## RESUME HERE (set 2026-07-30, updated after board redraw)

When the user says **"start"**, pick up at this exact point — do NOT restart the conversation from scratch.

**Where we ended:** All 12 owner blockers answered and written into `4.0 StockLens_UX_Ideate_and_UserFlows.md`. Section 14 rewritten to the **mode-switch model**. **Figma persona board STnYSkFCyFJKO6myJsFGHG redrawn to match (2026-07-30) — DONE.** Doc + memory + board all consistent. See [[stocklens-validation-status]] and [[stocklens-figma-userflows]] (holds the new node state).

**Board redraw — COMPLETE.** Board https://www.figma.com/board/STnYSkFCyFJKO6myJsFGHG now shows the mode-switch model: Anwar-Setup gate nodes un-greyed + relabelled to onboarding → dashboard mode switch (Warehouse ↔ Real Estate) → flip to Real Estate → bulk unit generator; Rahim = Explore→transfer + category filters + every-PO-needs-approval (auto-send branch deleted); Anwar-Daily bell = notification centre launches; Salam unchanged. Full node map in [[stocklens-figma-userflows]].

**THE NEXT TASK = wireframes.** Start the wireframe pass — **Items list spine first** — built to the owner decisions: scan-adds-stock, own-location default + Explore, one adaptive table + admin mode switch, smart-fields-by-type, category+filter. Validation interviews (`5.0 StockLens_UX_Validation_InterviewGuide.md`) can run in parallel.

**Still-open (not blockers):** partial-vs-ordered receiving mismatch (Decision 6, resolve in wireframe pass); single-approver backup/delegate/escalation (S8, Phase 2); Viewer internal-vs-client-facing (Q21, decide during design).

**Canonical doc:** `4.0 StockLens_UX_Ideate_and_UserFlows.md` (Section 12 "Resolved" + Decision 12 + rewritten Section 14). No emoji/icons anywhere (user preference).
